
/** Broadly, a thing that can happen to / be done by a character */
export class Consequence {
    /**
     * 
     * @param {string} raw_text The raw text of this consequence. E.x: "Dash 3"
     */
    constructor(raw_text) {
        this.raw_text = raw_text;
    }

    // 
}